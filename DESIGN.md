# pixel4ai 设计说明

这份文档讲 pixel4ai **怎么工作、为什么这样设计**。怎么用见 [README.md](README.md)，给 AI 的操作指南见 [skills/pixel-art/SKILL.md](skills/pixel-art/SKILL.md)。

---

## 1. 要解决的问题

大语言模型不能直接输出图片。让它逐像素写 RGB 值不现实，写 SVG 又不是像素风。像素画天然是离散的：画面是有限个格子，每格一种颜色，颜色来自一个很小的调色板。这种结构正好能用文本精确表达。

所以 pixel4ai 的思路是：

1. **把画布定义成「格子 × 调色板」的文本文档。** AI 能读能写，人也能用编辑器看着渲染效果改。
2. **AI 通过一组有校验、有回显的命令改文档**，而不是直接手写整个文件。
3. **人和 AI 改的是同一个文件**，文件是唯一的数据来源，两边都不持有私有状态。

---

## 2. 核心约束：一格一色

### 2.1 数据模型

```
Canvas
├── w, h                 宽高（格子数），1..1024
├── palette: {字符: "#rrggbb" | None}
│     "." → None         透明，固定存在，不能删改
└── grid: h 行 × w 列，每格一个调色板字符
```

「每个格子只能填一种颜色」不是靠绘制时检查来保证的，而是**数据结构本身就无法表达第二种颜色**：一格只存一个字符。所有操作最终都是把某些格子的字符换成调色板里的另一个字符。

### 2.2 为什么用单个字符表示颜色

| 候选 | 问题 |
| --- | --- |
| 每格存 RGB | 同一种颜色写几十遍，调色板无法统一修改；token 量大 |
| 每格存调色板下标（0、1、12…） | 下标位数不固定，行宽和画布宽对不上，等宽字体下无法对齐阅读 |
| **每格一个字符** | 一行字符串的长度正好等于画布宽度，等宽显示时上下对齐，文件本身就是 ASCII 画；字母可以助记（`k` 黑、`r` 红） |

代价是调色板最多 79 色（52 个字母、10 个数字、17 个符号），对像素画来说足够。

### 2.3 保留字符和字符集

- `.`：透明，永远在调色板里。
- `?`：只在 `stamp` / `rows` 的行数据里出现，表示「这格保持原样」，不能作为颜色。
- 允许作为颜色的字符：字母、数字和 `#@%&*+=~^$:;<>|/!`。
  - 不含 `-`：`-kk-` 这样的行数据会被命令行解析器当成选项。
  - 不含引号和反斜杠：在 shell 里很难写对。

### 2.4 坐标约定

`(x, y)` 从 0 开始，原点在左上角，x 向右、y 向下；**矩形和区域的两端都包含**（`rect 2 2 5 5` 是 4×4）。整个项目只有这一种约定：命令行参数、视图坐标尺、编辑器状态栏、选区复制出去的文本全都一样，AI 和人说的坐标可以直接互用。

---

## 3. 文件格式 `.pxl`

```json
{
  "version": 1,
  "size": [6, 3],
  "palette": {
    ".": null,
    "k": "#111111"
  },
  "rows": [
    ".kkkk.",
    "k....k",
    ".kkkk."
  ]
}
```

- **合法 JSON，但序列化格式是固定的**（`Canvas.to_json`）：每行 `rows` 独占一行，并且上下对齐。这样文件在文本编辑器里就能看出图形，`git diff` 也能按行显示改了哪些像素行。
- **载入时严格校验**（`Canvas.from_dict`）：行数必须等于高、每行长度必须等于宽、每个字符都必须在调色板里、颜色必须是 6 位十六进制。文件损坏时报出具体是哪一行、哪个字符，而不是悄悄修复。
- **原子写入**（`atomic_write`）：先写同目录下的临时文件 `.name.PID.tmp`，再 `os.replace` 覆盖。读者（编辑器、另一个命令）要么读到旧文件，要么读到新文件，永远不会读到写了一半的内容。

---

## 4. 架构

```
   AI（Claude Code + pixel-art Skill）                 人
            │  pxl 文件 命令 … / apply 脚本               │  看渲染效果、点格子
            ▼                                              ▼
   bin/pxl → cli.py                              bin/pxl-edit → editor.py
   载入 → 改副本 → 摘要 → 记历史 → 原子写         最新内容 + 这一笔 → 记历史 → 原子写
            │                                              ▲  │
            ▼                                              │  ▼
        ┌──────────────── 画.pxl（唯一数据来源）────────────────┐
        │   .画.pxl.history/  撤销历史（两边共用）               │
        └───────────────────────────────────────────────────────┘
                     每 300ms 比较文件字节，变了就刷新 ┘

   core.py   Canvas 模型、绘图操作、校验、文件读写、History
   render.py 坐标尺视图、缩略视图、PNG / ASCII / ANSI 导出
```

| 模块 | 职责 | 依赖 |
| --- | --- | --- |
| `core.py` | 数据模型、全部绘图和变换操作、校验、原子写、撤销历史 | 仅标准库 |
| `render.py` | 把画布变成给 AI 看的文本（带坐标尺）和给人看的图片 | 仅标准库 |
| `cli.py` | 命令解析、批量脚本、改动摘要和回显 | 仅标准库 |
| `editor.py` | PyQt6 可视化编辑器 | PyQt6 |
| `skills/pixel-art` | 告诉 AI 工作流程、命令写法和像素画规则 | — |

**为什么是无状态命令行，而不是常驻服务或 MCP：**
- 任何能执行 shell 的 AI 客户端都能直接用，不需要启动或管理进程。
- 每条命令都从文件读、往文件写，不存在「服务里的状态和文件不一致」这类问题。
- 用户在编辑器里的修改天然会被下一条命令看到。

以后要支持不能执行 shell 的客户端，可以在 CLI 外面再包一层 MCP，核心不用改。

---

## 5. AI 侧：命令行设计

### 5.1 一条命令的执行流程（`cli.dispatch`）

```
载入文件（before）
  → 在副本上执行命令（core 里的操作，失败抛 PxlError，文件不动）
  → 比较 before / after：
       完全相同 → 打印「没有变化」，不写文件、不记历史
       不同     → history.push(before) → 原子写 after
  → 打印改动摘要「改动 N 格 (x0,y0)-(x1,y1)」和视图
```

### 5.2 为什么提供几何命令，而不是只让 AI 写行

LLM 在长字符串里**数不准列号**，这是整个设计里最主要的约束。所以：

- 大块结构用几何命令表达：`rect`、`ellipse`、`line`、`fill`、`replace`、`mirror`、`flip`、`shift`。AI 只需要给出端点坐标，具体落在哪些格子由程序计算。
- 局部细节用 `stamp X Y` 贴一小块子网格。行可以不等长，`?` 表示不动，这样 AI 不用去数整行。
- `rows` 整行覆盖只适合很小的画布，而且**强制每行长度等于画布宽**，长度不对就直接拒绝。

### 5.3 反馈回路

AI 没有眼睛（或者不一定有），每一步都需要一个可以核对的结果：

1. **改动摘要**：`改动 36 格 (1,1)-(14,14)`。格数或范围不对，就说明坐标写错了。这一行不依赖 AI 去读图。
2. **带坐标尺的视图**：顶部是列号（十位只在整十处标出），左侧是行号。需要逐格核对时加 `--spaced`，格子之间加空格，让每个格子更可能被切成独立的 token。
3. **可操作的错误信息**：说清楚哪个值越界、合法范围是多少、应该怎么改。比如：
   `rows 第 0 行（y=2）'kkk' 长 3，应正好等于画布宽 12`、
   `颜色 'z' 不在调色板里（可用：. k w；要新增用 palette set z RRGGBB）`。
4. **操作要么全做要么不做**：`px` 先校验所有点再写，`stamp` 先校验所有行再写，失败时画布不会停留在做了一半的状态。

### 5.4 批量脚本 `apply`

```
pxl 画.pxl apply <<'EOF'
# 注释
rect 0 0 9 9 k --fill
stamp 2 2
k?k
end
EOF
```

- 每行用 `shlex` 切分，交给和命令行完全相同的解析器，所以脚本语法和单条命令一致。
- 以 `#` 开头的行是注释。命令总是以字母开头，所以参数里的 `#`（比如 `palette set # 222222`）不会被误判成注释。
- 如果 `stamp` / `rows` 那一行没有给行数据，就继续往下读到单独一行 `end` 为止；块内每一行都当作数据，不识别注释。
- **原子性**：整个脚本都在内存副本上执行，每行记一条改动摘要。任何一行出错，就报出行号和原因，文件保持不变；全部成功后只保存一次、只记一步撤销历史。
- 脚本里不能出现 `apply`、`undo`、`redo`、`edit`。

### 5.5 绘图算法

| 操作 | 算法 | 说明 |
| --- | --- | --- |
| `line` | Bresenham，包含两端点 | 像素画里最常用的直线，步长规律 |
| `ellipse` | 以格子中心为坐标，判断 `((x-cx)/ex)² + ((y-cy)/ey)² ≤ 1`，其中 `ex = (x1-x0)/2 + 0.3` | 半径外扩 0.3 格：不外扩的话，3×3、5×5 这类小圆会变成菱形或缺角的方块。外扩后 7×7 正好是像素画里常见的圆形 |
| 椭圆边框 | 取实心椭圆里「至少有一个上下左右邻居在外面」的格子 | 这样得到的轮廓是 8 连通的单像素线，没有加粗的拐角 |
| `fill` | 4 连通 BFS | 对角相邻的区域不会漏过去，和像素画描边的习惯一致 |
| `mirror lr/rl/tb/bt` | 把一半复制到另一半，奇数宽/高时中线不动 | 对称的精灵只画一半再镜像，最不容易出错 |
| `resize` | 9 个锚点，原内容按锚点贴进新尺寸 | 超出部分裁掉，新增部分透明 |
| `shade poly` 的形状 | 格子中心按奇偶规则判断是否在多边形内，再补上各条边（Bresenham） | 细长的投影形状也不会断开 |
| `shade` | 每格沿自己的色阶（`ramps`）移动 N 档，到头为止；`--soft N` 时，离形状边缘 d 格（d ≤ N）的格子只在 Bayer 4×4 阈值 < 16·(d − h)/(N+1) 时才处理，h 是按坐标算出的固定伪随机数（0–1） | 投影下面往往有多种颜色，按色阶映射就能一次压暗；边缘密度随距离递减。只用 Bayer 阈值时，一条直边只会用到矩阵的某一行，渐隐会变成「一行有、一行没有」的规整图案，加上 h 才能打散 |
| `shade --extend` | 色阶到头时，从端点颜色派生一个新颜色接上：更暗的亮度 ×0.78、色相向 250°（蓝紫）最多偏 9°；更亮的亮度向白靠拢 35%、色相向 50°（暖黄）最多偏 9°；饱和度 ×0.92 | 地面往往已经是色阶里最暗的颜色，不补颜色影子根本压不下去（dusk_cabin 演示：木屋投影从改动 220 格变成 1440 格） |
| `shade` 多个形状 | 一条命令里可以连写多个形状，先取并集再统一处理一次 | 同一光源的接触阴影和投影必然重叠。分成多条命令时，重叠处会被重复压暗；加了 `--extend` 还会连续补色（实测分 6 条画，一次补了 11 个颜色）。补色后某个色阶达到 7 档以上时，回显会给出警告 |
| `curve` | 经过所有点的均匀 Catmull-Rom 样条；每段按长度采样，相邻采样点用 Bresenham 连接，再去掉 L 形拐角 | AI 给「曲线经过哪些点」比摆贝塞尔控制点可靠；去掉 L 形拐角后，台阶处不会两格重叠。回显每列最上面一格的 y（高度剖面），放置物体时照着定底边 |
| `curve` 起伏警告 | 曲线横跨半个画布以上、起伏却不到画布高度的 3% 时警告 | dusk_cabin 的天空交界用 ±2.5 格的正弦扰动，256 列宽上只起伏 4–8 格，看起来仍是直线 |
| `curve` 竖直边警告 | 带填充时检查两个端点所在的列：本曲线的颜色和外侧相邻列不同的格子超过 3 个就警告 | 局部山脊（`--x0 / --x1`）直接往下填，端点会留下一条竖直的「悬崖」。演示中 `--x0 120` 的山脊就出现了这种直边 |
| `horizon` | 在 y0→y1 的斜线上，每段放一个 ±wave 的大起伏点，段与段中间再放一个「相邻大起伏的平均值 ± 0.35·wave」的小起伏点，交给 `curve`；随机数用固定种子 | 默认 wave 是画布高度的 6%，保证起伏看得出来（最初是 4%，用默认参数画的远山露出来的一段只起伏 6 格，被 `inspect` 报成近似直线，于是调高，让默认值能通过自己的检查）；同一个种子结果可复现，回显等价的 `curve` 命令方便微调 |
| `inspect` 交界 | 每列先用 7 格竖直窗口取多数颜色（平滑掉抖动），找上下各连续 ≥3 格的切换点，按「A→B」配对串成交界；只统计横跨 ≥50% 宽度的。起伏取 10%–90% 分位差，混合带宽度是交界附近 A、B 上下相邻的行跨度 | 逐行比较会被抖动打散：外部方案的逐行规则在 dusk_cabin 上命中 0 行，而这个方法能报出天空交界近似直线。只有同一材质（同一色阶，或没声明色阶时亮度差 <40）的交界才检查「没有过渡带」，山轮廓和天空之间本来就该清晰 |
| `inspect --object` 接触阴影 | 物体范围由 AI 指定。比较底边下方 3 行的平均亮度和同一高度、物体两侧 5–24 格地面的平均亮度（两侧取不到时，改用正下方 6–12 行），暗 8 以上算有接触阴影。不写 `--object` 就不检查，`apply` 后的摘要也不含这一项 | 在 dusk_cabin 上：原图木屋差 −2（没有阴影），加了阴影的副本差 −18，区分清楚；加了阴影的两根树干约 −20。已知局限：很细的物体脚下地面本身明暗不一，原图左树干就差 −12，会漏报。之所以不自动识别物体，是因为下面这一版在真实场景里不可靠 |
| （已放弃）自动识别物体 | 按材质求 4 连通块并分类：贴左右边、宽度 ≥30%、面积 ≥3%、不贴顶、下边缘在画布高度 60% 以下的是地面；不是地面但贴顶、或贴左右边又很宽的是背景（天空色带）；其余是物体。互相接触的物体块合成一个整体（屋顶 + 墙 + 门），每一列只看这个物体在该列最下面一格是否和地面相接：下方 3 行的地面平均亮度比这块地面的平均亮度低 6 以上才算有阴影，不到 40% 的列有阴影就报。最初只按面积和宽度判断地面，结果天空色带被当成地面、云被报成缺阴影，木屋侧墙反而被当成地面漏报；屋檐伸出墙外也被误报，所以加了背景分类和合并物体。之后又遇到两个问题：一是 64 格高的小场景里，同一色阶的天空一直延伸过 60% 高度，被当成地面，所以地面还要求不贴顶；二是 dusk_cabin 的木屋和门前小路连成一个物体，整体底部落在小路末端，只看整体底部时墙根反而没被检查，所以改成逐列看最下面一格 | 物体和地面相接处没有变暗，是「物件之间不像在一个图层」的主要原因。这是启发式判断，声明色阶后更准（带纹理的地面才能合成一块） |
| `fill --boundary` | BFS 跨过任何颜色扩散，碰到边界字符停下；先算出整个区域，`--closed` 且碰到画布边时报错，否则再统一填色 | 轮廓里常有草叶、花瓣等杂色，同色油漆桶填不满。天空、地面本来就挨着画布边缘，所以碰边只报告、不报错；想确认轮廓围住了才加 `--closed` |

---

## 6. 人侧：编辑器

编辑器存在的理由很直接：**肉眼改 ASCII 非常困难**，人需要看着真实颜色直接点格子。

### 6.1 渲染管线（`CanvasView.paintEvent`）

```
Canvas ──canvas_image()──► QImage（1 格 = 1 像素，RGBA8888）   ← 缓存，画布对象变了才重建
                                  │
       露出区域 e.rect() → 换算成格子范围 [cx0..cx1] × [cy0..cy1]
                                  │
     ┌─ 透明棋盘格（与格子对齐）───┤
     ├─ drawImage(子区域 → 放大 z 倍，最近邻)
     ├─ 未提交的笔画：复制子区域，逐像素叠加
     ├─ 网格线（z ≥ 6；每 5 格一条深线）
     ├─ 选区虚线框、悬停格高亮
     └─ 坐标尺：贴在可见区域上边和左边
```

要点：

- **缓存**：`canvas_image` 按行把字符映射成 4 字节颜色后拼起来，一次生成整张 1:1 图。每次修改都会产生新的 `Canvas` 对象，所以按对象身份判断缓存是否失效，悬停、拖动时不会重建。
- **只画看得见的部分**：512 格放大 8 倍就是 4096px 宽。每次只把露出区域对应的那块子图放大绘制，所以重绘耗时和画布大小基本无关，512 和 1024 下都在 15ms 左右。
- **最近邻放大**：`drawImage` 不开平滑，像素边缘保持锐利。
- **笔画预览**：拖动过程中，笔画只存在 `pending` 字典里；绘制时复制可见子图，把这些格子覆盖上去。松开鼠标才一次性提交，所以一笔只对应一次保存、一步撤销。
- **坐标尺吸附**：坐标尺画在可见区域的上边和左边，滚动时触发整块重绘，尺子始终留在视野里。刻度间隔按「数字宽度能放下」来选（1、2、5、10、20…）。点在尺子覆盖的位置不会画到被挡住的格子上（`cell_at` 会排除这块区域）。
- **透明棋盘格要和格子对齐**。这里有两个坑，都踩过：
  1. 棋盘格边长必须和格子边长互相整除（`checker_cell_size`）：格子大就切成 2×2、4×4 块（每块不超过 16px），格子小就几个格子拼成一块（至少 4px）。
  2. Qt 的纹理画刷默认从**控件**左上角开始平铺，而画布从坐标尺之后才开始，所以要给画刷加上 `(RULER, RULER)` 的平移（`cell_checker`）。

### 6.2 交互

- 工具：铅笔、橡皮、油漆桶、吸管、直线、矩形、椭圆、选区。右键始终是擦除，Alt+左键始终是吸色。
- 直线和椭圆直接复用 `core` 里的 `line_cells` / `ellipse_cells`，矩形按和 `Canvas.rect` 相同的规则计算，所以**编辑器画出来的形状和 AI 用命令画的完全一致**。
- 左右对称：每个落笔点都会同时加上它的镜像点 `(w-1-x, y)`。
- **选区是人向 AI 传递坐标的通道**：拖出选区后，`(x0,y0)-(x1,y1)` 会复制到剪贴板。直接贴给 AI，它就知道是 `--region x0 y0 x1 y1`。
- 调色板面板里每个色块都标着它的字符。人在编辑器里新增颜色时要指定一个字符，因为 AI 靠这个字符来指代这个颜色。

---

## 7. 协作与一致性

### 7.1 文件是唯一的数据来源

命令行不常驻，编辑器也不和命令行通信。两边唯一的交集就是磁盘上的 `.pxl` 文件和旁边的历史目录。

### 7.2 编辑器怎么发现外部修改（`Editor.check_disk`）

每 300ms 读一次文件字节，和「自己最后一次知道的字节」比较：

- **正在拖动时跳过**，等松手提交时再处理。
- 字节不同就解析；解析失败（比如用户手改坏了）就保留当前画面，下一轮再试，同一条错误不重复提示。
- 解析成功且内容确实不同（`Canvas.same`），就刷新画面。

**为什么不用修改时间，也不用 inotify / QFileSystemWatcher：**
- 最早的实现是比较 `(mtime, size)`。测试中出现过偶发失败：编辑器刚保存完，外部就在同一个时间戳精度内又改了一格，文件大小不变，这次修改被漏掉了。
- 原子写是「新文件替换旧文件」，inode 会变，文件监听会跟丢，还得反复重新挂上。
- 文件最大也就 1MB 左右（1024×1024），每 300ms 读一次、比较一次字节，开销可以忽略，而且绝对可靠。

### 7.3 编辑器提交一笔（`Editor.mutate`）

```
before = latest()        文件字节没变 → 直接用内存里的画布；变了 → 重新解析磁盘内容
after  = before 的副本 + 这一笔的格子（越界的、颜色已被删掉的格子会被丢弃）
不同 → history.push(before) → 原子写 after → 记下写出去的字节
```

关键在于**提交时以磁盘上的最新内容为底**，而不是以编辑器开始拖动时的画面为底。AI 在用户拖动期间做的修改会保留，用户这一笔只覆盖它实际画到的格子（按格子级别，后写的生效）。

### 7.4 撤销历史共用

命令行和编辑器用的是同一个 `History`，所以 AI 的一步和人的一笔在同一个撤销栈里，顺序就是实际发生的顺序。

---

## 8. 撤销历史（`core.History`）

```
.画.pxl.history/
├── index.json          {"undo": [3, 4, 7], "redo": [8], "next": 9}
├── 3.json.gz           每步一个完整快照（gzip level 1）
├── 4.json.gz
└── …
```

- `push(before)`：写入一个新快照，id 追加到 undo；清空 redo 并删除对应文件；超过 50 步就删掉最旧的。
- `step(current, n, "undo"|"redo")`：从源栈顶读出快照作为新状态，同时把当前状态存成快照压到另一个栈。**先写 index，再删不再引用的快照文件**，这样中途崩溃最多留下无用文件，不会出现 index 指向缺失的快照。
- **为什么存完整快照而不是差异**：实现简单，任何一步都能独立恢复。像素画大面积是同色，gzip 压缩率很高：实测 1024×1024 画布做 4 次大面积修改和 1 次撤销后，历史目录约 56KB。
- **为什么每步单独一个文件**：最早是所有快照放进一个 JSON，512×512 下 50 步就有十几 MB，每改一笔都要整个读写。拆开后每次操作只碰一个快照。
- 旧版单文件格式 `.画.pxl.history.json` 第一次读取时会自动迁移成目录格式。

---

## 9. 大画布

上限是 1024×1024，512×512 要求流畅。难点有三个：给 AI 的文本不能爆掉、历史不能越改越慢、编辑器不能卡。

### 9.1 缩略视图（`render.overview`）

区域超过 64 行或列时，文本视图自动缩略：

- 倍数 `step = ceil(最长边 / 64)`，每个字符代表 `step × step` 个格子。
- 每块显示**块内最多的非透明颜色**；整块都透明才显示 `.`。这样 1 格粗的轮廓线在缩略图里也不会消失。
- 行号仍然是原图的 y。列号竖着写，每列从上往下读出来就是原图的 x，AI 不需要自己换算。
- 修改后的回显：画布放得下就显示整张；放不下就只显示改动范围外扩 2 格；改动范围本身也太大，就对这个范围再缩略。实测 512 和 1024 画布上任何操作的回显都不超过约 72 行。

缩略视图只适合看布局，精确坐标要用 `view --region` 看不超过 64×64 的逐格视图。能看图片的 AI 应该导出 PNG 来看，这一点写在了 Skill 里。

### 9.2 性能手段

| 位置 | 手段 |
| --- | --- |
| 校验 | 按行做集合比较：行里的字符集合是调色板的子集就直接通过，不逐格检查 |
| `bbox` | 每行用 `lstrip` / `rstrip` 找出非透明内容的两端 |
| `diff` | 整行相同直接跳过 |
| 比较画布 | `Canvas.same`：比较 grid 列表，不先转成 dict/JSON |
| PNG | 不带网格线时，每行把颜色字节映射后 join，再整行重复 `scale` 次 |
| 编辑器 | 缓存 1:1 图、只画露出区域、文件没变时不重新解析 |

实测（本机）：命令行各操作在 512 画布上 0.06–0.17 秒，1024 画布上 0.09–0.78 秒；编辑器重绘约 15ms，提交一笔或撤销 0.02–0.07 秒。

---

## 10. 导出

- **PNG**（`render.png_bytes`）：只用标准库手写，RGBA 8 位，每行过滤字节为 0，zlib 压缩。按 `scale` 做最近邻放大（直接重复字节）。`--grid` 在格子之间插入 1px 半透明灰线，此时输出宽度为 `w×scale + w + 1`。输出边长上限 8192px。
- **ASCII**：默认原样输出调色板字符，可以再导回来；`--ramp` 按亮度（Rec.709 系数）映射到 `@%#*+=-:. `，每格横向写两个字符，抵消终端字符的高宽比。
- **ANSI**：用上半块字符 `▀`，前景色是上面一格、背景色是下面一格，每个终端字符显示两格，24 位真彩色。

---

## 11. Skill 的作用

工具只保证「做得到、做对了会告诉你」，**怎么画好**写在 Skill 里。Skill 的内容来自实际画图时踩过的坑：

- **按图层分批 `apply`，每层画完先看视图再继续。** 第一次画样例蘑菇时，所有图层写在一个脚本里，高光坐标是猜的，结果盖掉了描边。改成先画轮廓、看过视图、再按实际的内部范围放细节之后，一格都没错。
- 对称物体只画一半再 `mirror`。
- 大画布先按区域规划坐标，用几何命令搭结构，用 `--region` 核对细节，能看图就导出 PNG 看。
- 像素画规则：1px 描边、固定左上光源、避免 pillow shading（沿轮廓一圈圈变暗）和锯齿斜线、控制颜色数量。
- 协作习惯：动手前先 `view`，因为用户可能刚在编辑器里改过。

### AI 怎么找到工具

SKILL.md 里所有命令都写成 `${CLAUDE_SKILL_DIR}/scripts/pxl`。Claude Code 加载 Skill 时，会把这个变量替换成 Skill 的实际目录。`skills/pixel-art/scripts/pxl` 用 `resolve()` 顺着 `~/.claude/skills/pixel-art` 的软链接找到真实位置，往上三层就是项目根目录，再把 `src/` 加进导入路径。所以 AI 调用既不依赖 PATH，也不用用户告诉它项目装在哪。

入口脚本找项目的顺序：环境变量 `PIXEL4AI_HOME` → 脚本真实位置往上三层 → `~/Projects/pixel4ai`。都找不到时，会报错并提示运行 `install.sh` 或设置 `PIXEL4AI_HOME`。

如果 AI 是直接读 SKILL.md 文件，而不是通过 Skill 加载的，变量不会被替换。SKILL.md 里为这种情况按顺序写了备用调用方式。

**Skill 必须先被发现。** Claude Code 在**进程启动时**扫描 `~/.claude/skills`，扫描代码接受软链接目录（`isDirectory() || isSymbolicLink()`）。但后台任务用的是守护进程预先启动好的备用进程，它们的 Skill 列表停留在进程启动的那一刻。

2026-09-10 的实际排查：Skill 在 20:07 安装。另一个窗口 20:13 开始的后台会话，所在进程 19:23 就启动了（守护进程日志记录它 19:37 被认领）。那个会话的 `skill_listing` 里没有 pixel-art，AI 不知道有这个工具，就自己写脚本画图，直到用户说明路径才改用 pxl。

对应的处理：
- `install.sh` 和 README 提示装完新开会话。
- Skill 描述里覆盖「像素风、场景、风景、大尺寸」等说法，并写明「不要自己另写脚本生成图片」。
- `tests` 里校验 frontmatter 能被解析，并在不带 PATH、HOME 指向临时目录的环境下，通过软链接运行入口脚本。

### 参考库：内容放项目，Skill 只放入口

像素抖动这类参考资料（效果图 + 描述）放在项目的 `refs/<库>/` 下，每个库有一个 `catalog.json`。Skill 里只写几行说明，告诉 AI 用 `pxl ref` 按需查询（`src/pixel4ai/refs.py`）。

这样做的目的是省算力：放在 Skill 目录里的文件本来不会自动占用上下文，但 AI 一旦列目录、逐个读文件，开销就上去了。命令查询把「AI 能拿到什么」限制在一小段里：

| 查询 | 内容 | 上限 |
| --- | --- | --- |
| `pxl ref dither` | 按场景选择 + 规则 + 反例（来自 `catalog.json` 的 `digest`） | 30 行 |
| `pxl ref dither <条目>` | 做法、禁忌、预览图绝对路径和读图的 token 估算 | 20 行（`--full` 不限） |
| `pxl ref dither --find 关键词` | 匹配的条目和场景，每条一行 | 20 条 |

预览图边长限制在 384px 以内，`--check` 会校验。AI 只在需要看效果时，读条目给出的那一张图。

`digest` 是 `refs/dither/README.md` 第 3、4 节的精简版。测试会核对两边的场景、规则、反例条数是否一致，防止只改了一边。

---

## 12. 测试策略

`tests/test_pixel4ai.py`，用 `QT_QPA_PLATFORM=offscreen python3 -m unittest discover -s tests` 运行：

- **core**：序列化往返、损坏文件的拒绝、小圆的精确形状、各操作的结果、越界和调色板错误信息、全做或不做、撤销重做、快照数量上限、旧格式迁移。
- **render**：坐标尺格式、缩略视图的行数、列号和细线可见性、解码 PNG 检查像素、ASCII / ANSI 输出。
- **CLI**（子进程）：脚本里的 `#` 颜色和数据块、脚本出错时文件不变、`-q`、拒绝覆盖已有文件、大画布回显长度。
- **编辑器**（离屏驱动）：模拟鼠标画一笔 → 外部修改后自动刷新 → 再画一笔不丢外部改动 → 撤销；大画布滚动后点击画对格子、点在坐标尺上不画；所有缩放倍数下棋盘格对齐；以脚本方式直接运行。

**「反向验证」测试本身**：修 bug 时，把 bug 临时放回去跑一遍，确认测试确实会失败。棋盘格对齐测试就是这样发现了自己的漏洞：最初只在每块中心取样，后来改成对角线上取样，这两种都查不出「平铺起点错开」；最终改成在每块四个角附近分别取样才能发现。

---

## 13. 已知限制

- **没有文件锁。** 命令行的「读 → 改 → 写」和编辑器的提交如果在同一瞬间（毫秒级）交错，后写入的一方会覆盖先写入的一方。编辑器只在提交那一刻以磁盘最新内容为底；命令行不做合并。被覆盖的内容可以从撤销历史里找回。
- **编辑器发现外部修改最多有 300ms 延迟**，并且每轮都要读整个文件。
- **每步撤销存完整快照。** 颜色非常杂乱的大图压缩率低，历史目录会明显变大。
- **`apply` 脚本里的 `export` 会立即写出文件**，即使后面的行出错、画布没有保存，已经导出的文件也不会回滚。
- **文本视图对大画布的帮助有限。** 缩略视图只能看布局，512 以上的大图要靠 AI 能看 PNG。
- 调色板最多 79 色。
- 还没有：多边形填充、多帧动画、PNG 导入（量化到调色板）、MCP 封装。

这些限制的处理计划和优先级见 [TODO.md](TODO.md)。
