# pixel4ai 待办清单

来源：[DESIGN.md](DESIGN.md) 第 13 节「已知限制」、「接入 Aseprite / Piskel 工作流」的讨论，以及 ASCII 导出的改进讨论。

**优先级判断标准**：是否阻塞核心工作流、是否有数据丢失风险、是否显著扩展可用场景、实现成本。

状态：☐ 待做　◐ 进行中　☑ 完成　✕ 不做

> 标「核实」的备注是对照代码后修正或补充的事实，写于 2026-09-10。

---

## P0 — 必须尽快做

| 状态 | 功能 | 为什么 | 依赖 / 备注 | 工作量 |
|---|---|---|---|---|
| ☐ | **导出 Aseprite（.ase/.aseprite）最小骨架** | pxl 从「自洽工具」变成「工作流环节」的关键。没有它，AI 生成得再准也只能导 PNG，用户还得手动导入、切图、设锚点。 | 只需支持 Indexed 模式、单层、单帧、带调色板；像素数据用 zlib 压缩。可参考 aseprite-io 或官方格式规范。<br>核实：`.` 透明需要映射到 Aseprite 的透明色索引（Indexed 模式下通常是 0），调色板顺序按 `.pxl` 里的 palette 顺序。 | 中 |
| ☐ | **CLI 写前并发检查（乐观锁）** | 目前 CLI 的「读→改→写」和编辑器提交毫秒级交错时，后写会覆盖先写。AI 批量 apply 可能持续数秒，期间用户改一笔就会丢。 | 载入时记录文件字节/哈希，写前重新读取比较；不一致就拒绝并提示重试。成本低，收益高。<br>核实：`cli.dispatch` 开头载入、最后 `core.save`，中间没有任何检查；`apply` 在整个脚本跑完才保存，窗口最长。编辑器提交时已经以磁盘最新内容为底，只需要改 CLI。 | 小 |
| ☐ | **修复 apply 脚本内 export 的原子性** | 脚本里 export 会立即写文件，即使后面的行出错、画布没保存，导出的文件也不会回滚，导致 AI 拿到的 PNG 和磁盘画布不一致。 | 方案一：延迟 export 到脚本全部成功后；方案二：禁止脚本内 export，提示用单独命令。<br>建议方案一：导出的是「脚本执行到那一行时的画面」，延迟时要先把那一刻的画布副本存下来。 | 小 |

## P1 — 重要，显著提升可用性

| 状态 | 功能 | 为什么 | 依赖 / 备注 | 工作量 |
|---|---|---|---|---|
| ☐ | **导出 Piskel（.piskel）** | Piskel 是免费网页端编辑器，.piskel 是 JSON + base64 PNG，实现简单。覆盖另一大用户群。 | pxl 已能生成 PNG（`render.png_bytes`），构造 JSON 即可。注意 Piskel 对调色板支持有限，可能丢命名。 | 小 |
| ☐ | **MCP 封装** | 让不能执行 shell 的 AI 客户端（如 Claude Desktop）也能用。CLI 已无状态，包一层 MCP 即可。 | 核心不用改，只做适配层。 | 中 |
| ☐ | **调色板上限处理与导入/导出** | 新增颜色满时行为未明确；调色板无法单独保存/复用。 | 满时明确报错并提示删除；支持 palette export/import JSON，方便跨文件复用。<br>核实：上限是 **79 色**（52 字母 + 10 数字 + 17 符号），不是约 70。另一个更实际的问题：`palette set` 对已存在的字符会**直接覆盖颜色**，不报错，AI 想新增颜色时容易误改已有颜色，应加提示或 `--overwrite`。编辑器新增颜色时只从 62 个字母数字里自动挑。 | 小 |
| ☐ | **.pxl 版本迁移策略** | 现在 `version: 1`，将来改格式需要迁移。 | 预留 `migrate_v1_to_v2` 钩子；载入时按 version 分派。<br>核实：当前 `Canvas.from_dict` **完全不读 `version`**，新版本的文件不会报错，而是可能被按 v1 悄悄解析。先加「版本高于当前支持就明确报错」，再做迁移钩子。 | 小 |

## P2 — 有用，但不紧急

| 状态 | 功能 | 为什么 | 依赖 / 备注 | 工作量 |
|---|---|---|---|---|
| ☐ | **ASCII 导出改进** | `export ascii --ramp` 实测有几个问题：很亮的颜色（白斑、菌柄内部）映射到空格，和透明分不开；深色终端上明暗反了；512 宽的画每行 1024 字符；字符不能自定义。 | ① 不透明颜色不用空格，透明才是空格；② `--invert` 反转明暗；③ `--width N` 先缩小再输出（可复用缩略视图的取块逻辑 `render.block_char`）；④ `--map 'k=#,w=o'` 自定义映射。实现在 `render.ascii_art`。 | 小 |
| ☐ | **半透明颜色（调色板带 alpha）** | 现在透明只有两种状态：`.` 全透明，其余全不透明。玻璃、阴影、光晕、柔和边缘做不了。 | 调色板接受 `#rrggbbaa`；PNG 直接写 alpha；编辑器渲染本来就是 RGBA，主要改解析和调色板面板；ASCII `--ramp` / ANSI 要约定阈值（比如 alpha < 128 当透明）；Aseprite 导出时调色板带 alpha。<br>核实：`parse_color` 只接受 6 位十六进制，`palette set g 00ff0080` 实测报错（2026-09-10）。 | 中 |
| ☐ | **`--grid` 网格线不画在透明区域** | 带网格的 PNG 里，网格线一律是 alpha=110 的灰色，透明区域也有，不透明色块之间也夹着半透明缝，只能当预览、不能当素材。 | 方案：只在线两侧至少一格不透明时画线；或加选项控制。实现在 `render.png_bytes` 的 grid 分支。<br>核实：蘑菇 `--grid` 导出实测 alpha 分布为 0 / 110 / 255 三种（2026-09-10）。 | 小 |
| ☐ | **PNG 导入（量化到调色板）** | 从参考图/现有素材生成 pxl，是自然需求。 | 需要量化算法（median cut / k-means），效果难保证，建议作为实验功能。 | 大 |
| ☐ | **大画布文本视图区域查询** | 512 以上 AI 不能看图时基本「半盲」，缩略图只能看布局。 | 增加查询命令，让 AI 主动查局部而非看全图。<br>核实：已有 `info`（整体内容范围、每色格数）和 `view --region`（局部逐格）。真正缺的是：按颜色查范围（`query bbox k`）、查单格（`query at X Y`）、按行给游程（`query row Y` → `0-49 . / 50-120 k …`），游程比逐格视图省 token。 | 中 |
| ☐ | **编辑器外部修改检测优化** | 目前 300ms 轮询读整个文件，1MB 内可接受，但不够优雅。 | 核实：「大小 + 哈希」省不了多少，算哈希照样要读整个文件；只比 mtime + 大小正是之前漏检的做法（同一时间戳精度内只改一格）。可行方案：先比 `(inode, mtime_ns, size)`，都没变就跳过读取（原子写每次都会换 inode，能覆盖 pxl 自己的写入）；再每隔几秒兜底完整比较一次，覆盖原地改写的外部编辑器。 | 小 |
| ☐ | **多边形填充** | 绘图操作扩展。画地形、山体这类不规则大块时比较有用。 | 现有 rect/ellipse/line/fill 已覆盖大多数场景，优先级低。 | 中 |

## P3 — 锦上添花，或明确不建议

| 状态 | 功能 | 建议 | 理由 |
|---|---|---|---|
| ✕ | **多帧动画** | 明确不做，建议在 Aseprite 里做 | 数据模型要大改；Aseprite 动画功能成熟，pxl 保持单帧更简单。 |
| ✕ | **导入 Aseprite** | 不建议 | 双向同步复杂，且 pxl 定位是「AI 生成 → 导出 → 人在 Aseprite 精修」的单向入口。 |
| ✕ | **导出 GIF/APNG** | 等有多帧动画再说 | 当前无动画需求。 |
| ✕ | **Unicode 字符集扩展** | 不建议 | 破坏等宽对齐和 shell 友好性，79 色对像素画足够。 |
| ☐ | **透明像素颜色外扩（alpha bleeding）** | 可选 | 透明像素导出为 (0,0,0,0)。像素画按惯例用最近邻缩放，没有影响；但在 Unity / Godot 等引擎里开了双线性过滤时，边缘会混进黑色出现暗边。可加 `export png --bleed`：用相邻不透明像素的 RGB 填充透明像素（alpha 仍为 0）。 |
| ☐ | **FUNGI.EXE 从 .pxl 加载精灵** | 可选 | 原 README「之后可以加的」里的一项：用 pxl 画 `~/Projects/spores` 的精灵，替代 `fungi.py` 里代码生成的蘑菇。两边的精灵格式本来就一致（字符网格 + 字符→颜色）。 |

---

## 参考库：像素抖动（refs/dither）

起因：AI 生成图的边缘和过渡抖动过于刻板。另一个窗口画的 256×256 风景里，天空色带交界是等宽的直线「花边」，太阳光晕是固定半径的同心环，雪线锯齿周期完全一致。根源是抖动全靠 `(x+y)%2`、固定 Bayer 阈值这类公式直接算出来。

设计：真实素材只放在项目的 `refs/dither/`；Skill 里只加几行说明，AI 通过 `pxl ref dither [条目]` 按需查询，输出有长度上限，避免读入整个库浪费算力。

| 状态 | 阶段 | 内容 | 怎么算完成 |
|---|---|---|---|
| ☑ | 1 | `refs/dither/` 骨架、`README.md` 规则与反例、`catalog.json` 字段定义；纳入用户提供的参考渐变条（知乎 @三二） | 用户审阅规则是否符合想要的风格<br>2026-09-10 已交付：目录骨架、README（术语 / 场景选择 / 7 条规则 / 反例 / 参考条解析 / 字段规范）、catalog.json（第一个条目 `reference-strip`）、SOURCES.md；参考条逐格还原并逐像素校验一致。用户 2026-09-10 审阅通过。 |
| ☑ | 2 | `pxl ref` 命令 + SKILL.md 加链接 | 输出不超过行数上限；不带 PATH 时经 Skill 入口也能查到<br>2026-09-10 已交付：`src/pixel4ai/refs.py`（库列表 / 概要 / 单条目 / `--find` / `--list` / `--check`）、`catalog.json` 的 `digest`、SKILL.md 新增「抖动、渐变与柔和边缘」一节、8 个测试。实测概要 23 行、单条目 16 行，经 Skill 入口在 `/tmp` 下运行正常。 |
| ☐ | 3 | `build.py` 生成素材：5 类可平铺图案、3 条过渡带、7 组刻板 / 自然对比场景 | 目录和文件一致、实际密度和声明相符、能无缝平铺、预览图是最新的 |
| ☐ | 4 | 新开会话让 AI 按参考库重画风景画的天空交界和太阳光晕，对比同一位置 | 用户判断是否不再刻板 |
| ☐ | 5 | （可选）`pxl dither` / `pxl gradient` 命令 | 前 4 步证明有效后再做 |

默认选择（用户未另行指定）：首批约 25 个条目；自然版先由 `build.py` 生成，再由用户在编辑器里修一遍后定为标准范例。

---

## 建议实施顺序

1. **P0 三项**：导出 Aseprite + 写前并发检查 + apply 内 export 原子性。
   这三项直接决定 pxl 能否进入真实工作流，以及会不会丢数据。
   建议先做后两项（小、且是数据正确性问题），再做 Aseprite 导出。
2. **P1 四项**：导出 Piskel + MCP + 调色板管理 + 版本迁移。
   扩展可用场景，补齐工程完整性。版本检查那半项（高版本明确报错）很小，可以跟 P0 一起做。
3. **P2 选做**：ASCII 导出改进、PNG 导入量化、大画布查询等。
   根据用户反馈决定是否投入。

一句话总结：**先把「导出 Aseprite」和「不丢数据」做掉，pxl 就从演示工具变成了工作流环节；其余都是在这个基础上的增强。**
