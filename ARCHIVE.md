# pixel4ai 归档说明

**状态：已于 2026-09-15 归档，不再开发。** 代码、测试和文档保持归档时的原样，供以后参考。

## 为什么停止

用户在 2026-09-15 决定放弃这个项目：GPT-6 Astra 已经可以通过 Lua 接口直接在 Aseprite 里作画。pixel4ai 的编辑器、`.pxl` 格式和基础绘图命令，本质上是在复刻 Aseprite 这类成熟编辑器早已具备的能力，继续投入的意义不大。

（GPT-6 Astra 的这项能力由用户告知，本项目没有做过验证。）

## 归档时的状态

- **测试**：58 个全部通过（2026-09-15，Debian 13，Python 3.13）。
- **已完成**：
  - `pxl` 命令行：一格一色的网格画布、绘图和变换命令、原子性的批量脚本、撤销历史、PNG / ASCII / ANSI 导出、大画布缩略视图
  - PyQt6 编辑器：和命令行共用同一个文件，外部修改自动刷新，撤销栈共用
  - Claude Code Skill `pixel-art`，以及可重复执行的安装脚本 `install.sh`
  - 抖动参考库 `refs/dither`（阶段 1、2）和 `pxl ref` 查询命令
  - 复杂场景修正方案第 1–4 项：
    - 色阶 `ramp` 和阴影命令 `shade`（多形状合并、边缘渐隐、`--extend` 补色）
    - `curve` / `horizon` / `fill --boundary`
    - `inspect` 构图自检和 `view --outline`
- **未完成，归档时全部停止**：见 [TODO.md](TODO.md)。包括复杂场景方案第 5 项 `stamp --from`、第 6 项验收，抖动参考库阶段 3–5，以及 P0–P3 各项。
- **已卸载**：`~/.local/bin/pxl`、`~/.local/bin/pxl-edit`、`~/.claude/skills/pixel-art` 三个软链接。项目文件没有删除。

## 值得留下的结论

以下几点都是在开发过程中用真实数据验证过的，和用什么工具画无关。

1. **AI 画像素画的主要问题出在审美决策，不在像素对齐。**
   dusk_cabin（256×256）的排查结果：
   - 天空交界横跨全宽，起伏只有 2–4 格（按 10%–90% 分位计算）
   - 同一材质的色带之间，混合带宽度中位数只有 2 行
   - 物体脚下没有接触阴影，调色板里也没有准备阴影色

   给模型再精确的绘图接口，这些问题也不会自动消失。
2. **没有阴影色，影子根本画不出来。**
   - 在 dusk_cabin 上画投影时，范围内有 1438 格已经是色阶里最暗的颜色，实际只压暗了 220 格。
   - 在色阶暗端自动补一个颜色（`shade --extend`）后，改动变成 1440 格。
   - 同一光源的多个阴影形状必须合并起来一次处理。分成 6 条命令画时，重叠处被反复压暗，还一次补出了 11 个颜色。
3. **±2 格的正弦扰动，在 256 画布上看起来仍然是直线。** 交界起伏至少要达到画布高度的 3%，自动生成时默认用 6% 比较稳妥。
4. **逐行检测条带会被抖动打散。** 一份外部方案提出的逐行检测规则，在 dusk_cabin 上命中 0 行。先竖直平滑、再逐列找交界，才检测得出来。
5. **在层层遮挡的场景里，自动识别「物体」和「地面」不可靠。**
   - 三版启发式规则都失败了：要么把云和天空碎块当成物体，要么把木屋漏掉。
   - 接触阴影检查应该由画的人指定物体范围。按这种方式，没加阴影的木屋差 −2，加了阴影的差 −18，区分得很清楚。
6. **Claude Code 的后台任务使用预先启动的进程，Skill 列表在进程启动那一刻就定下了。**
   - 2026-09-10 的排查：Skill 于 20:07 安装；另一个后台会话 20:13 才开始，但它所在的进程 19:23 就已启动，所以没有加载这个 Skill，AI 自己写了脚本去画图。
   - 结论：安装 Skill 之后必须新开会话。
7. **给模型的参考资料要按需查询、限制输出长度，并在 Skill 里写明用法。** 实测 AI 会自己把输出接 `head` 截断、猜条目名，结果查询失败。

## 如果以后要恢复

```bash
cd ~/Projects/pixel4ai
./install.sh                                                       # 重新创建 3 个软链接并自检
QT_QPA_PLATFORM=offscreen python3 -m unittest discover -s tests    # 确认环境可用
```

恢复后要新开 Claude Code 会话，才会加载 Skill。

归档前讨论过、但没有执行的转型方向，恢复时可以参考：不再维护编辑器，改做 Aseprite 之上的质量工具。
- 先做 `pxl import`，让 `inspect` 可以检查任意 PNG
- 再做 `export aseprite`
- Skill 增加一条 Aseprite 路线
- 用「加不加规则和自检」的对照实验，验证这层工具到底有没有价值

## 相关文件

- [DESIGN.md](DESIGN.md)：设计取舍和各项算法说明
- [TODO.md](TODO.md)：归档时的待办清单和排查记录
- [refs/dither/README.md](refs/dither/README.md)：抖动参考库
- [refs/dither/SOURCES.md](refs/dither/SOURCES.md)：参考图出处（知乎 @三二）。原图只保存在本地、不进版本库；由原图还原出的文件，公开发布前需要确认授权。
